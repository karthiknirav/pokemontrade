[build]
builder = "NIXPACKS"
buildCommand = "npm install && prisma generate && next build"

[deploy]
startCommand = "prisma migrate deploy && next start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
